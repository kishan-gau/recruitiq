# Simple production Dockerfile for web app
# This assumes the app has already been built locally (dist folder exists)

FROM nginx:alpine

# Install runtime dependencies for health checks
RUN apk add --no-cache curl

# Copy nginx configuration
COPY nginx/frontend.conf /etc/nginx/conf.d/default.conf

# Copy pre-built application from local dist folder
COPY apps/web/dist /usr/share/nginx/html

# Add health check
HEALTHCHECK --interval=30s --timeout=3s --start-period=10s --retries=3 \
  CMD curl -f http://localhost/health || exit 1

# Expose port
EXPOSE 80

# Start nginx
CMD ["nginx", "-g", "daemon off;"]
