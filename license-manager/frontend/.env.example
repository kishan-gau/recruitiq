# License Manager Backend API (using Vite proxy)
# Leave as /api to use relative URLs - proxy configured in vite.config.js
VITE_API_URL=/api

# Deployment Service API (TransIP OpenStack Integration)
# Leave as /api to use same backend
VITE_DEPLOYMENT_API_URL=/api
