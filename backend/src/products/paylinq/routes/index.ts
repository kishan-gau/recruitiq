/**
 * Paylinq Product Module Entry Point
 * Exports routes for the Dynamic Product Loader
 */

import routes from './routes.ts';

// Export routes as default for ProductLoader
export default {
  routes
};
