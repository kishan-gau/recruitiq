# TransIP API Configuration
# Copy this file to .env and fill in your actual credentials
# Get these from: https://www.transip.nl/cp/account/api/

# TransIP API Username (your TransIP account username)
TRANSIP_USERNAME=your_transip_username

# TransIP API Private Key (path to private key file or key content)
# Option 1: Path to private key file
TRANSIP_PRIVATE_KEY=/path/to/transip_private_key.pem

# Option 2: Or paste the private key content directly
# TRANSIP_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\nMIIEvQ...your_key_here...\n-----END PRIVATE KEY-----"

# Environment (affects test mode and billing)
NODE_ENV=development  # Use 'production' for real VPS provisioning

# Optional: TransIP API Settings
# TRANSIP_READ_ONLY=false     # Set to true to prevent any modifications
# TRANSIP_TEST_MODE=true      # Set to true to add ?test=1 to API calls
# TRANSIP_ALLOW_BILLING=false # Set to true to enable operations that create invoices

# =====================================
# How to Get TransIP API Credentials
# =====================================

# 1. Log into your TransIP control panel: https://www.transip.nl/cp/
# 2. Go to: Account → API
# 3. Click "Create new key pair"
# 4. Give it a name (e.g., "RecruitIQ Production")
# 5. Enable "Allow whitelist-only" for security (optional but recommended)
# 6. Click "Generate"
# 7. Download the private key file (keep it secure!)
# 8. Copy your username from the control panel

# =====================================
# Security Best Practices
# =====================================

# ✅ DO:
# - Store private key file outside the repository
# - Use file permissions: chmod 600 transip_private_key.pem
# - Use separate keys for dev/staging/production
# - Rotate API keys every 90 days
# - Enable IP whitelisting in TransIP control panel

# ❌ DON'T:
# - Commit .env file to version control
# - Share private keys via email or chat
# - Use the same key across all environments
# - Store keys in plaintext in production

# =====================================
# Testing Configuration
# =====================================

# For development/testing (no real VPS provisioning):
NODE_ENV=development
TRANSIP_USERNAME=test_user
TRANSIP_PRIVATE_KEY=fake_key_for_testing

# For production (real VPS provisioning):
NODE_ENV=production
TRANSIP_USERNAME=your_real_username
TRANSIP_PRIVATE_KEY=/secure/path/to/transip_private_key.pem
